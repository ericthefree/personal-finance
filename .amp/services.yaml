services:
  web:
    command: .venv/bin/waitress-serve --listen=0.0.0.0:"$PORT" run:app
    health: /
    portal:
      title: Personal Finance
      description: Review the local personal checking dashboard.
